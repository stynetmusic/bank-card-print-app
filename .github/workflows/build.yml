name: Build Windows EXE (x64) - Python 3.8.10

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python 3.8.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.8.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install "numpy<2.0.0" "PyQt6==6.6.1" "PyQt6-Qt6==6.6.1" "PyQt6-sip==13.6.0" Pillow reportlab pyinstaller
    
    - name: Build EXE
      run: pyinstaller --noconfirm build.spec
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: UF_Print_Cards_Windows_x64_Python38
        path: dist/UF_Print_Cards_App/
        retention-days: 30
